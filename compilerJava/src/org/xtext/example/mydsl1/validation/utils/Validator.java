package org.xtext.example.mydsl1.validation.utils;

import org.xtext.example.mydsl1.validation.utils.ValidatorRepository;

public class Validator {
	public ValidatorRepository validator;
	public Validator(ValidatorRepository validator) {
		this.validator = validator;
	}
}
